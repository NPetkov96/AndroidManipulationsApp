<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MedSestriManipulations.MainPage"
             BackgroundColor="#F2F4F7">

    <ScrollView>
        <VerticalStackLayout Padding="20" Spacing="15">

            <!-- Бутоните -->
            <HorizontalStackLayout HorizontalOptions="Center" Spacing="15">
                <Button Text="Изпрати"
                        Clicked="OnSendClicked"
                        BackgroundColor="#007BFF"
                        TextColor="White"
                        CornerRadius="10"
                        Padding="15,10" />

                <Button Text="Зачисти"
                        Clicked="OnClearClicked"
                        BackgroundColor="DarkRed"
                        TextColor="White"
                        CornerRadius="10"
                        Padding="15,10"/>
            </HorizontalStackLayout>

            <!-- Полета за данни -->
            <Label Text="Данни за пациент:" FontSize="22" FontAttributes="Bold"/>
            <Entry x:Name="CurrentName" Placeholder="Име" FontSize="20" BackgroundColor="White"/>
            <Entry x:Name="EGNEntry" Placeholder="ЕГН" Keyboard="Numeric" FontSize="20" BackgroundColor="White"/>
            <Entry x:Name="PhoneEntry" Placeholder="Телефон" Keyboard="Telephone" FontSize="20" BackgroundColor="White"/>
            <Entry x:Name="UIN" Placeholder="Лекар УИН" Keyboard="Numeric" FontSize="20" BackgroundColor="White"/>

            <!-- Изследвания -->
            <Label Text="Изследвания" FontSize="22" FontAttributes="Bold"/>
            <SearchBar x:Name="SearchBar" Placeholder="Търси манипулация..." TextChanged="OnSearchTextChanged" BackgroundColor="White"/>

            <!-- Списък с процедури -->
            <CollectionView x:Name="ProcedureList" ItemsSource="{Binding Procedures}" HeightRequest="500" Margin="0,0,0,10">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Frame Margin="0,5" CornerRadius="12" HasShadow="True" BackgroundColor="White">
                            <StackLayout Orientation="Horizontal" VerticalOptions="Center">
                                <CheckBox IsChecked="{Binding IsSelected}"
                                          CheckedChanged="OnProcedureCheckedChanged"
                                          Color="#007BFF"
                                          Scale="1.4"
                                          VerticalOptions="Center"/>

                                <StackLayout Margin="10,0" VerticalOptions="Center">
                                    <StackLayout.GestureRecognizers>
                                        <TapGestureRecognizer Tapped="OnProcedureLabelTapped" />
                                    </StackLayout.GestureRecognizers>
                                    <Label Text="{Binding Name}" FontSize="18" TextColor="Black"/>
                                </StackLayout>

                                <Label Text="{Binding Price, StringFormat=' {0:F2} лв'}"
                                       FontSize="18"
                                       FontAttributes="Bold"
                                       TextColor="Gray"
                                       VerticalOptions="Center"
                                       HorizontalOptions="EndAndExpand"/>
                            </StackLayout>
                        </Frame>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>

            <!-- Общо -->
            <Label x:Name="TotalLabel"
                   Text="Общо: 0.00 лв"
                   FontSize="24"
                   FontAttributes="Bold"
                   TextColor="#007BFF"
                   HorizontalOptions="Center"
                   Margin="0,10,0,0"/>
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>


<!--<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MedSestriManipulations.MainPage"
             >


    <ScrollView>
        <VerticalStackLayout Padding="20" Spacing="10">

            <HorizontalStackLayout>
                <Button Text="Изпрати" Clicked="OnSendClicked" BackgroundColor="#007BFF"/>
                <Button Text="Зачисти" Clicked="OnClearClicked" Margin="10,0,0,0" BackgroundColor="DarkRed"/>
            </HorizontalStackLayout>

            <Label Text="Данни за пациент:" FontSize="20"/>
            <Entry x:Name="CurrentName" Placeholder="Име" FontSize="25"/>
            <Entry x:Name="EGNEntry" Placeholder="ЕГН" Keyboard="Numeric" FontSize="25"/>
            <Entry x:Name="PhoneEntry" Placeholder="Телефон" Keyboard="Telephone" FontSize="25"/>
            <Entry x:Name="UIN" Placeholder="Лекар УИН" Keyboard="Numeric" FontSize="25"/>

            <Label Text="Изследвания" FontSize="20"/>
            <SearchBar x:Name="SearchBar" Placeholder="Търси манипулация..." TextChanged="OnSearchTextChanged"/>

            <CollectionView x:Name="ProcedureList" ItemsSource="{Binding Procedures}" HeightRequest="500" Margin="0,0,0,10">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <StackLayout Orientation="Horizontal" Padding="0,5,0,0">
                            <CheckBox IsChecked="{Binding IsSelected}" CheckedChanged="OnProcedureCheckedChanged"/>
                            <StackLayout Margin="5,7,0,0">
                                <StackLayout.GestureRecognizers>
                                    <TapGestureRecognizer Tapped="OnProcedureLabelTapped" />
                                </StackLayout.GestureRecognizers>
                                <Label Text="{Binding Name}" FontSize="20" />
                            </StackLayout>
                            <Label Text="{Binding Price, StringFormat=' {0:F2} лв'}" Margin="0,7,0,0" FontSize="20"/>
                        </StackLayout>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>

            <Label x:Name="TotalLabel" Text="Общо: 0.00 лв" FontSize="20" FontAttributes="Bold" HorizontalOptions="Center" Margin="0,10,0,10"/>

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>-->
